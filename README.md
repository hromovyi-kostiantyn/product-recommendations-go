# Рекомендаційна система продуктів

![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-316192.svg)
![Docker](https://img.shields.io/badge/Docker-Enabled-2496ED.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

Інформаційна система формування продуктових рекомендацій на основі методів машинного навчання та колаборативної фільтрації.

## 📋 Зміст

- [Опис проєкту](#-опис-проєкту)
- [Архітектура](#-архітектура)
- [Функціональність](#-функціональність)
- [Технічний стек](#-технічний-стек)
- [Встановлення та запуск](#-встановлення-та-запуск)
- [API ендпоінти](#-api-ендпоінти)
- [Алгоритми рекомендацій](#-алгоритми-рекомендацій)
- [Структура проєкту](#-структура-проєкту)
- [Розробка](#-розробка)
- [Документування коду](#-документування-коду)
- [Ліцензія](#-ліцензія)

## 🚀 Опис проєкту

Цей проєкт реалізує REST API для інтелектуальної рекомендаційної системи товарів інтернет-магазину. Система аналізує історію взаємодій користувачів з товарами (перегляди, лайки, покупки) та формує персоналізовані рекомендації на основі різних алгоритмів:

- Колаборативна фільтрація (аналіз схожості між користувачами)
- Контентна фільтрація (аналіз категорій товарів)
- Фільтрація на основі популярності
- Гібридний підхід, що поєднує кілька методів

Система дозволяє користувачам реєструватися, авторизуватися, переглядати каталог товарів, додавати товари до вподобань, здійснювати замовлення та отримувати персоналізовані рекомендації.

## 🏗️ Архітектура

Проєкт побудований за принципами чистої архітектури з розділенням на шари:

- **Шар доставки** (Delivery Layer) - обробка HTTP запитів, перевірка вхідних даних
- **Сервісний шар** (Service Layer) - бізнес-логіка та обробка даних
- **Шар репозиторію** (Repository Layer) - доступ до бази даних

Використовується підхід Dependency Injection для зменшення зв'язків між компонентами та підвищення тестованості.

## 🔧 Функціональність

- **Аутентифікація користувачів** - реєстрація, логін, logout з використанням JWT
- **Каталог товарів** - перегляд списку товарів з пагінацією, отримання деталей товару
- **Система вподобань** - додавання та видалення товарів з лайків
- **Система замовлень** - створення замовлень, перегляд історії
- **Рекомендації** - отримання персоналізованих рекомендацій на основі вподобань та покупок

## 💻 Технічний стек

- **Мова програмування**: Go 1.21+
- **База даних**: PostgreSQL 16
- **ORM**: GORM
- **Веб-фреймворк**: Gorilla Mux
- **Аутентифікація**: JWT (JSON Web Tokens)
- **Контейнеризація**: Docker і Docker Compose
- **Live Reload**: Air
- **Документація API**: Swagger (опціонально)

## 📥 Встановлення та запуск

### Передумови

- Docker і Docker Compose
- Git

### Клонування репозиторію

```bash
git clone https://github.com/username/product-recommendations-go.git
cd product-recommendations-go
```

### Налаштування змінних оточення

Створіть файл `.env` у кореневій директорії:

```
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=recommendations
APP_PORT=8080
JWT_SECRET=your_jwt_secret_key
```

### Запуск за допомогою Docker Compose

```bash
# Запуск всіх сервісів
docker-compose up -d

# Перевірка статусу контейнерів
docker-compose ps

# Перегляд логів
docker-compose logs -f
```

### Запуск міграцій та наповнення бази

Міграції та наповнення бази даними виконуються автоматично при запуску через Docker Compose. Якщо потрібно запустити їх окремо:

```bash
# Запуск міграцій
docker-compose run migration

# Запуск наповнення бази
docker-compose run seeder
```

## 🔌 API ендпоінти

### Аутентифікація

- `POST /api/v1/auth/register` - реєстрація нового користувача
  ```json
  {
    "email": "user@example.com",
    "password": "password123"
  }
  ```

- `POST /api/v1/auth/login` - вхід у систему
  ```json
  {
    "email": "user@example.com",
    "password": "password123"
  }
  ```

- `POST /api/v1/auth/logout` - вихід із системи (потребує токена)

### Товари

- `GET /api/v1/products?page=1&limit=10` - отримання списку товарів з пагінацією
- `GET /api/v1/products/{id}` - отримання інформації про конкретний товар

### Вподобання

- `POST /api/v1/likes/{product_id}` - додавання товару до вподобань
- `DELETE /api/v1/likes/{product_id}` - видалення товару з вподобань
- `GET /api/v1/likes` - отримання списку вподобаних товарів

### Замовлення

- `POST /api/v1/orders` - створення нового замовлення
  ```json
  {
    "items": [
      {
        "product_id": 1,
        "quantity": 2
      },
      {
        "product_id": 3,
        "quantity": 1
      }
    ]
  }
  ```

- `GET /api/v1/orders` - отримання списку замовлень користувача
- `GET /api/v1/orders/{id}` - отримання інформації про конкретне замовлення

### Рекомендації

- `GET /api/v1/recommendations?limit=10` - отримання персоналізованих рекомендацій

### Статус сервісу

- `GET /api/v1/health` - перевірка статусу сервісу

## 🧠 Алгоритми рекомендацій

Система використовує гібридний підхід до формування рекомендацій, що включає:

1. **Колаборативна фільтрація** - аналіз поведінки схожих користувачів для рекомендації товарів
2. **Контентна фільтрація** - аналіз категорій товарів, які цікавлять користувача
3. **Фільтрація за популярністю** - рекомендація найпопулярніших товарів
4. **Випадкові рекомендації** - для нових користувачів без історії взаємодій

Кожна рекомендація супроводжується рейтингом, який вказує на ступінь відповідності вподобанням користувача.

## 📂 Структура проєкту

```
product-recommendations-go/
├── cmd/                        # Точки входу в програму
│   └── api/                    # Код API сервера
├── internal/                   # Приватні пакети проєкту
│   ├── config/                 # Конфігурація додатка
│   ├── container/              # Dependency Injection контейнер
│   ├── delivery/               # Шар доставки (HTTP обробники)
│   ├── models/                 # Структури даних (моделі)
│   ├── repository/             # Шар доступу до даних
│   └── service/                # Бізнес-логіка
├── pkg/                        # Публічні пакети
│   └── recommendation/         # Алгоритми рекомендацій
├── migrations/                 # Міграції бази даних
├── scripts/                    # Скрипти наповнення даними
├── docker/                     # Файли для контейнеризації
├── LICENSE                     # Ліцензія MIT
├── README.md                   # Цей файл
├── .env                        # Змінні оточення (не у git)
├── .gitignore                  # Файли, що ігноруються git
├── .air.toml                   # Конфігурація live reload
├── go.mod                      # Залежності Go
├── go.sum                      # Хеші залежностей
└── docker-compose.yml          # Конфігурація Docker Compose
```

## 🛠️ Розробка

### Запуск в режимі розробки з автоматичним перезавантаженням

Проєкт використовує [Air](https://github.com/air-verse/air) для автоматичного перезавантаження при зміні коду:

```bash
# Запуск API в режимі розробки
docker-compose up api
```

### Корисні Docker команди

```bash
# Перебудова контейнерів
docker-compose up -d --build

# Показати логи
docker-compose logs -f api

# Зупинка всіх контейнерів
docker-compose down

# Виконання команди в контейнері
docker-compose exec api sh
```

### Тестування API

Для тестування API рекомендується використовувати API-клієнт, наприклад:
- [Postman](https://www.postman.com/)
- [Insomnia](https://insomnia.rest/)
- або curl у терміналі:

```bash
# Приклад запиту на реєстрацію
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'
```

## 📝 Документування коду

### Стандарти документування

У проєкті ми дотримуємося стандартів документування Go:

1. **Документація пакетів** - кожен пакет повинен починатися з документаційного коментаря:

```go
// Package repository реалізує шар доступу до даних,
// відповідальний за взаємодію з базою даних PostgreSQL
// через ORM GORM.
package repository
```

2. **Документація експортованих елементів** - усі експортовані (з великої літери) типи, функції, методи, змінні та константи повинні мати документаційні коментарі:

```go
// User представляє модель користувача системи.
type User struct {
    ID       int64  `json:"id" gorm:"primaryKey"`
    Email    string `json:"email" gorm:"unique"`
    Password string `json:"-"`
}

// FindByEmail повертає користувача за його електронною адресою.
// Повертає помилку, якщо користувача не знайдено.
func (r *UserRepository) FindByEmail(email string) (*User, error) {
    // ...
}
```

3. **Документація складних алгоритмів** - для складних алгоритмів слід додати детальний опис принципу роботи:

```go
// PredictRating прогнозує рейтинг, який користувач може дати певному товару
// на основі оцінок інших користувачів використовуючи колаборативну фільтрацію.
//
// Алгоритм працює в такі етапи:
// 1. Знаходить користувачів, які оцінили цільовий товар
// 2. Обчислює подібність між цільовим користувачем і кожним з цих користувачів
// 3. Обчислює зважену суму оцінок товару, використовуючи подібність як ваги
//
// Параметри:
//   - targetUser: ідентифікатор користувача, для якого прогнозується рейтинг
//   - targetItem: ідентифікатор товару, для якого прогнозується рейтинг
//   - userRatings: карта оцінок користувачів (ключ - ID користувача, значення - карта оцінок)
//
// Повертає:
//   - прогнозований рейтинг товару для користувача
func PredictRating(targetUser, targetItem int64, userRatings map[int64]map[int64]float64) float64 {
    // ...
}
```

4. **Приклади використання** - для ключових компонентів слід створювати приклади використання:

```go
func ExampleRecommendationService_GetRecommendations() {
    service := NewRecommendationService(productRepo, likeRepo, orderRepo, recommendationRepo)
    recommendations, err := service.GetRecommendations(1, 5)
    fmt.Println(len(recommendations) <= 5)
    // Output: true
}
```

### Інструменти документування

Для роботи з документацією використовуються наступні інструменти:

1. **pkgsite** - сучасний інструмент для перегляду документації Go:

```bash
go install golang.org/x/pkgsite/cmd/pkgsite@latest
```

2. **godoc** - класичний інструмент для перегляду документації:

```bash
go install golang.org/x/tools/cmd/godoc@latest
```

3. **swag** - інструмент для генерації Swagger/OpenAPI документації:

```bash
go install github.com/swaggo/swag/cmd/swag@latest
```

### Генерація документації

Для генерації документації використовуйте:

```bash
# Запуск скрипта для генерації документації
./scripts/generate_docs.sh

# Перевірка якості документації
./scripts/check_docs.sh
```

Згенерована документація зберігається в директорії `docs/generated/`.

Для перегляду документації локально:

```bash
# За допомогою pkgsite
cd docs/generated && python -m http.server 8080

# Або за допомогою godoc
godoc -http=:6060
```

Документація API доступна за адресою: [https://hromovyi-kostiantyn.github.io/product-recommendations-go/api/](https://hromovyi-kostiantyn.github.io/product-recommendations-go/api/)

### Перевірка якості документації

Якість документації перевіряється за допомогою лінтерів:

```bash
# Перевірка документації експортованих елементів
golint ./...

# Перевірка загальної якості документації
./scripts/check_docs.sh
```

### Правила для нових контриб'юторів

При створенні нового коду в проєкті, дотримуйтесь наступних правил документування:

1. **Документуйте всі експортовані елементи** - кожен експортований тип, функція, метод, змінна або константа повинні мати документаційний коментар.

2. **Коментарі починаються з імені елемента** - коментар повинен починатися з імені елемента, який документується, і продовжуватися повним реченням.

3. **Описуйте параметри і повернені значення** - для функцій і методів документуйте параметри та значення, що повертаються.

4. **Додавайте приклади використання** - для складних компонентів створюйте приклади використання у вигляді тестів.

5. **Оновлюйте документацію при змінах коду** - при зміні функціональності обов'язково оновлюйте відповідну документацію.

6. **Використовуйте стандартний формат для пакетів** - кожен пакет повинен мати коментар-документацію, що описує його призначення.

7. **Документуйте архітектурні рішення** - для важливих архітектурних рішень створюйте окремі документи в директорії `docs/`.

### Автоматична генерація документації

Документація автоматично генерується та публікується при кожному коміті в гілки `main`, `master` або `develop`. Ви можете переглянути актуальну документацію за адресою:

[https://hromovyi-kostiantyn.github.io/product-recommendations-go/](https://hromovyi-kostiantyn.github.io/product-recommendations-go/)


## 📄 Ліцензія

Цей проєкт розповсюджується за ліцензією MIT. Дивіться файл [LICENSE](LICENSE.md) для отримання додаткової інформації.

---

Розроблено в рамках бакалаврської роботи на тему "Інформаційна система формування продуктових рекомендацій на основі методів машинного навчання", 2025.